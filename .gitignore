# ETL world output (docs/adr/002) — large, regenerable from Overture; never
# the serving DB, never committed.
data/etl/

# Local stand-in for the Supabase page_publication table
# (src/hotelareascore/publication.py) -- regenerable via
# scripts/compute_publication.py, never the real serving DB.
data/serving/

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
.venv/
venv/

# Env
.env

# OS
.DS_Store
