# Go-live sitemap dry run — consistency report

Generated by `scripts/dry_run_sitemaps.py` (night mission #2, Tache 3). Output XML: `docs/reports/dry-run-sitemaps/` -- not served, not built, not crawlable.

## Counts

- Static pages: 2 (expected 2: home, methodology)
- City pages: 12 (expected 12)
- Hotel pages: 200 (staged cohort: 200)
- **Total sitemap URLs if flipped today: 214**

## Consistency issues

None. Every staged URL resolves to a real built page in the committed snapshot.

## Caveat: sitemap route files are hand-maintained, not auto-derived

`web/src/pages/sitemap-static.xml.ts` hardcodes its path list by hand (same convention this script mirrors above) -- `sitemap-cities.xml.ts` was fixed this session to read `CITY_PAGES` dynamically instead (it previously hardcoded `[]`), matching `sitemap-hotels.xml.ts`'s existing pattern. If the go-live cohort or static-page list ever changes, update `sitemap-static.xml.ts` and this script together.
